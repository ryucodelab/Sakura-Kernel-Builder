name: Build Kernel (MediaTek)

on:
  workflow_dispatch:
    inputs:
      KERNEL_SOURCE:
        description: "Kernel source repo URL"
        required: true
        type: string
      KERNEL_BRANCH:
        description: "Kernel source branch"
        required: true
        type: string
      KERNEL_CONFIG:
        description: "Defconfig name (no path, e.g. lavender_defconfig)"
        required: true
        type: string
      KERNEL_NAME:
        description: "Kernel name used for file naming (e.g. Sakura)"
        required: false
        type: string
        default: "Sakura"
      KERNEL_VERSION:
        description: "Major kernel version (determines toolchain)"
        required: true
        type: choice
        options:
          - "4.4"
          - "4.14"
          - "4.19"
      CODENAME:
        description: "Device codename (e.g. lavender)"
        required: true
        type: string
      BOT_TOKEN:
        description: "Telegram Bot Token (optional)"
        required: false
        type: string
        default: ""
      CHAT_ID:
        description: "Telegram Chat ID (optional)"
        required: false
        type: string
        default: ""
      USE_ANYKERNEL3:
        description: "Use AnyKernel3?"
        required: true
        type: choice
        options:
          - "yes"
          - "no"
        default: "yes"
      PATCH_KERNELSU:
        description: "Patch KernelSU (optional)"
        required: false
        type: choice
        options:
          - "none"
          - "ksu-next"
          - "ksu-original"
        default: "none"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      KERNEL_SOURCE: ${{ github.event.inputs.KERNEL_SOURCE }}
      KERNEL_BRANCH: ${{ github.event.inputs.KERNEL_BRANCH }}
      KERNEL_CONFIG: ${{ github.event.inputs.KERNEL_CONFIG }}
      KERNEL_NAME: ${{ github.event.inputs.KERNEL_NAME }}
      KERNEL_VERSION: ${{ github.event.inputs.KERNEL_VERSION }}
      CODENAME: ${{ github.event.inputs.CODENAME }}
      BOT_TOKEN: ${{ github.event.inputs.BOT_TOKEN }}
      CHAT_ID: ${{ github.event.inputs.CHAT_ID }}
      USE_ANYKERNEL3: ${{ github.event.inputs.USE_ANYKERNEL3 }}
      PATCH_KERNELSU: ${{ github.event.inputs.PATCH_KERNELSU }}
      KBUILD_BUILD_USER: "github-actions"
      KBUILD_BUILD_HOST: "ci"

    steps:
      # ---------------------------------------------------------------
      # Record start time (used to compute build duration later)
      # ---------------------------------------------------------------
      - name: ⏱️ Record start time
        run: echo "START_EPOCH=$(date +%s)" >> $GITHUB_ENV

      # ---------------------------------------------------------------
      # NOTIFY: BUILD STARTED
      # ---------------------------------------------------------------
      - name: "📣 Telegram: Notify build start"
        if: env.BOT_TOKEN != '' && env.CHAT_ID != ''
        run: |
          KSU_LABEL="No"
          [ "$PATCH_KERNELSU" = "ksu-next" ] && KSU_LABEL="Yes (KernelSU-Next)"
          [ "$PATCH_KERNELSU" = "ksu-original" ] && KSU_LABEL="Yes (KernelSU Original)"

          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          MSG=$'🔨 *Build Started*\nKernel: `'"${KERNEL_NAME}"'`\nDevice: `'"${CODENAME}"'`\nKernel version: `'"${KERNEL_VERSION}"'`\nKernelSU: `'"${KSU_LABEL}"'`\nTriggered by: `${{ github.actor }}`'

          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
            -d chat_id="${CHAT_ID}" \
            -d parse_mode="Markdown" \
            --data-urlencode text="${MSG}" \
            --data-urlencode reply_markup="$(jq -n --arg url "$RUN_URL" \
              '{inline_keyboard: [[{text: "🔗 View Build Run", url: $url}]]}')"

      # ---------------------------------------------------------------
      # SETUP
      # ---------------------------------------------------------------
      - name: 🧹 Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL || true
          sudo docker image prune --all --force || true

      - name: 📦 Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential bc bison flex libssl-dev libelf-dev \
            git curl wget zip unzip python3 python-is-python3 \
            cpio kmod rsync ccache dwarves libncurses-dev \
            device-tree-compiler

          if [ "$KERNEL_VERSION" = "4.4" ] || [ "$KERNEL_VERSION" = "4.14" ]; then
            wget -q http://archive.ubuntu.com/ubuntu/pool/main/o/openssl1.0/libssl1.0.0_1.0.2n-1ubuntu5.10_amd64.deb || true
            sudo dpkg -i libssl1.0.0_1.0.2n-1ubuntu5.10_amd64.deb 2>/dev/null || true
          fi

      - name: 🗃️ Setup ccache
        run: |
          mkdir -p ~/.ccache
          echo "USE_CCACHE=1" >> $GITHUB_ENV
          echo "CCACHE_DIR=$HOME/.ccache" >> $GITHUB_ENV

      # ---------------------------------------------------------------
      # CLONE KERNEL SOURCE
      # ---------------------------------------------------------------
      - name: 📥 Clone kernel source
        run: |
          git clone --depth=1 --recurse-submodules --shallow-submodules -b "$KERNEL_BRANCH" "$KERNEL_SOURCE" kernel
          cd kernel
          # Fallback in case shallow-submodules fails (some submodules don't support shallow fetch)
          if git submodule status | grep -qE '^-'; then
            echo "Some submodules are not initialized yet, retrying without shallow..."
            git submodule update --init --recursive
          fi
          cd ..
          echo "KDIR=$(pwd)/kernel" >> $GITHUB_ENV
          echo "KERNEL_COMMIT=$(cd kernel && git rev-parse --short HEAD)" >> $GITHUB_ENV

      # ---------------------------------------------------------------
      # TOOLCHAIN: sweet spot clang + gcc based on KERNEL_VERSION
      # ---------------------------------------------------------------
      - name: 🛠️ Setup toolchain (clang + gcc)
        run: |
          mkdir -p toolchain/clang toolchain/gcc64 toolchain/gcc32

          case "$KERNEL_VERSION" in
            "4.4")  CLANG_BRANCH="9.0.0-release" ;;
            "4.14") CLANG_BRANCH="12.0.0-release" ;;
            "4.19") CLANG_BRANCH="14.0.0-release" ;;
          esac

          # Per-device override: merlin (Redmi Note 9, MT6769/Helio G85) ships
          # a 4.14.x tree that's late enough (4.14.356) to need a newer clang
          # than the generic 4.14 default above — community builds for this
          # exact device/kernel combo use clang 14, and 12.0.0 has been seen
          # to choke on newer intrinsics/attributes this tree's drivers use.
          if [ "$CODENAME" = "merlin" ]; then
            CLANG_BRANCH="14.0.0-release"
          fi
          echo "Using clang branch: $CLANG_BRANCH"

          git clone --depth=1 -b "$CLANG_BRANCH" https://github.com/ZyCromerZ/Clang toolchain/clang \
            || git clone --depth=1 https://github.com/kdrag0n/proton-clang toolchain/clang

          # AOSP GCC is needed as an assembler fallback for 4.4 & 4.14
          if [ "$KERNEL_VERSION" != "4.19" ]; then
            git clone --depth=1 -b android-9.0.0_r1 \
              https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 toolchain/gcc64
            git clone --depth=1 -b android-9.0.0_r1 \
              https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 toolchain/gcc32
          fi

          echo "$(pwd)/toolchain/clang/bin" >> $GITHUB_PATH
          echo "$(pwd)/toolchain/gcc64/bin" >> $GITHUB_PATH
          echo "$(pwd)/toolchain/gcc32/bin" >> $GITHUB_PATH

      # ---------------------------------------------------------------
      # PATCH KERNELSU (optional)
      # ---------------------------------------------------------------
      - name: 🔓 Patch KernelSU
        if: env.PATCH_KERNELSU != 'none'
        working-directory: kernel
        run: |
          if [ "$PATCH_KERNELSU" = "ksu-next" ]; then
            curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -
          elif [ "$PATCH_KERNELSU" = "ksu-original" ]; then
            curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -
          fi

      # ---------------------------------------------------------------
      # SANITY CHECK: prevent kconfig error "can't open file .../kernelsu/Kconfig"
      # This happens when drivers/Kconfig already references kernelsu (either
      # from a source that was already stitched together by the maintainer,
      # or from the setup.sh above) but the driver folder itself is missing
      # or failed to clone.
      # ---------------------------------------------------------------
      - name: 🩺 Sanity check KernelSU wiring
        working-directory: kernel
        run: |
          KSU_REF=$(grep -iE 'source[[:space:]]+["'"'"']?[^"'"'"']*kernelsu[^"'"'"']*/Kconfig' drivers/Kconfig 2>/dev/null || true)
          if [ -n "$KSU_REF" ]; then
            echo "KernelSU reference found in drivers/Kconfig: $KSU_REF"
            KSU_OK=0
            for d in drivers/kernelsu drivers/kernelsu-next KernelSU; do
              if [ -d "$d" ] && [ -n "$(ls -A "$d" 2>/dev/null)" ]; then
                KSU_OK=1
                echo "KernelSU driver folder found and populated: $d"
                break
              fi
            done
            if [ "$KSU_OK" = "0" ]; then
              if [ "$PATCH_KERNELSU" = "none" ]; then
                echo "::warning::KernelSU reference exists but the folder is empty/missing (submodule failed to init?). The source line was removed automatically so kconfig doesn't fail."
                sed -i '/kernelsu/Id' drivers/Kconfig
                sed -i '/kernelsu/Id' drivers/Makefile 2>/dev/null || true
              else
                echo "::error::PATCH_KERNELSU=${PATCH_KERNELSU} but the KernelSU driver folder (drivers/kernelsu, drivers/kernelsu-next, or KernelSU) is empty/missing."
                echo "::error::Check the 'Clone kernel source' (submodule) and 'Patch KernelSU' steps above."
                exit 1
              fi
            elif [ "$PATCH_KERNELSU" != "none" ]; then
              echo "::warning::This kernel source already ships with a built-in KernelSU (submodule), but PATCH_KERNELSU=${PATCH_KERNELSU} was also selected. This can cause conflicting/duplicate KSU drivers. If this was unintentional, set PATCH_KERNELSU=none on the next run."
            fi
          fi

      # ---------------------------------------------------------------
      # BUILD
      # ---------------------------------------------------------------
      - name: 🔨 Build kernel
        working-directory: kernel
        run: |
          export ARCH=arm64
          export SUBARCH=arm64

          COMMON_ARGS="-j$(nproc --all) O=out ARCH=arm64 CC=clang LD=ld.lld AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip"

          if [ "$KERNEL_VERSION" != "4.19" ]; then
            COMMON_ARGS="$COMMON_ARGS CROSS_COMPILE=aarch64-linux-android- CROSS_COMPILE_ARM32=arm-linux-androideabi- CLANG_TRIPLE=aarch64-linux-gnu-"
          else
            COMMON_ARGS="$COMMON_ARGS CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- CLANG_TRIPLE=aarch64-linux-gnu-"
          fi

          make $COMMON_ARGS "$KERNEL_CONFIG"

          # Fix for: "Cannot use CONFIG_CC_STACKPROTECTOR_STRONG/REGULAR:
          # -fstack-protector[-strong] not supported by compiler" during the
          # top-level Makefile's prepare-compiler-check target. This is NOT
          # actually about clang lacking stack-protector support — MediaTek
          # kernel Makefiles run that feature-detection with -Werror, and
          # their defconfigs carry GCC-only flags (e.g. -fconserve-stack,
          # -mno-android-relro) that clang doesn't recognize. Clang's "unknown
          # argument" warning gets promoted to an error by -Werror, so the
          # probe reports EVERY stack-protector variant as "unsupported" no
          # matter which one is selected (confirmed: switching STRONG -> REGULAR
          # just moved the failure to REGULAR). The only variant that skips the
          # probe entirely is NONE, since no flag needs to be feature-tested.
          # Fix for: "Unknown command line argument '-polly-loopfusion-greedy=1'"
          # (and similar '-polly-postopts=1' / '-polly-reschedule=1' errors).
          # CONFIG_POLLY_CLANG hardcodes -mllvm -polly-* flag NAMES that only
          # exist in the specific Polly build that config option was written
          # against. The ZyCromerZ/proton-clang toolchain here ships a
          # different Polly version where those flags were renamed (clang's
          # own "Did you mean" hints confirm this), so clang aborts with a
          # nonzero exit on the unrecognized argument. Simplest fix: disable
          # Polly rather than trying to pin an exact-matching clang build.
          if [ -f scripts/config ] && [ -f out/.config ]; then
            ./scripts/config --file out/.config \
              -d COMPAT_VDSO \
              -d CC_STACKPROTECTOR_STRONG \
              -d CC_STACKPROTECTOR_AUTO \
              -d CC_STACKPROTECTOR_REGULAR \
              -e CC_STACKPROTECTOR_NONE \
              -d POLLY_CLANG
            make $COMMON_ARGS olddefconfig
          fi

          make $COMMON_ARGS 2>&1 | tee build.log

          # MediaTek kernels often don't produce a combined Image.gz-dtb — many
          # ship the kernel Image and device tree blobs separately (dtb.img /
          # dtbo.img), unlike the single-blob layout common on other SoCs. Check
          # for the combined image first, then fall back to a bare Image.
          IMG=$(find out/arch/arm64/boot -type f -name "Image.gz-dtb" | head -n1)
          if [ -z "$IMG" ]; then
            IMG=$(find out/arch/arm64/boot -type f \( -name "Image.gz" -o -name "Image" -o -name "Image-dtb" \) | head -n1)
          fi

          if [ -z "$IMG" ]; then
            echo "Kernel image not found, build failed."
            exit 1
          fi

          echo "IMG_PATH=$(pwd)/$IMG" >> $GITHUB_ENV

          # dtb.img / dtbo.img: MediaTek boots typically flash these as separate
          # partitions from the kernel Image, so grab them if the build produced
          # them (harmless no-op if this source combines everything into IMG).
          DTB_IMG=$(find out/arch/arm64/boot -maxdepth 2 -type f -name "dtb.img" | head -n1)
          DTBO_IMG=$(find out -maxdepth 3 -type f -name "dtbo.img" | head -n1)
          [ -n "$DTB_IMG" ] && echo "DTB_IMG_PATH=$(pwd)/$DTB_IMG" >> $GITHUB_ENV
          [ -n "$DTBO_IMG" ] && echo "DTBO_IMG_PATH=$(pwd)/$DTBO_IMG" >> $GITHUB_ENV

      # ---------------------------------------------------------------
      # NAMING: KERNEL_NAME-CODENAME-DATE(-KSU/-KSU-NEXT)
      # ---------------------------------------------------------------
      - name: 🏷️ Build final filenames
        if: success()
        run: |
          DATE=$(date +%Y%m%d)

          case "$PATCH_KERNELSU" in
            ksu-next)     KSU_TAG="-KSU-NEXT" ;;
            ksu-original) KSU_TAG="-KSU" ;;
            *)            KSU_TAG="" ;;
          esac

          BASE_NAME="${KERNEL_NAME}-${CODENAME}-${DATE}${KSU_TAG}"
          echo "BASE_NAME=${BASE_NAME}" >> $GITHUB_ENV
          echo "RELEASE_TAG=build-${{ github.run_id }}" >> $GITHUB_ENV

      # ---------------------------------------------------------------
      # KERNELSU MANAGER APK: bundle the matching manager app with the release
      # - ksu-original -> pinned to v0.6.6 (last version compatible with old/legacy KernelSU)
      # - ksu-next     -> always fetched from the latest KernelSU-Next release
      # ---------------------------------------------------------------
      - name: 📱 Download KernelSU Manager APK
        if: success() && env.PATCH_KERNELSU != 'none'
        run: |
          mkdir -p manager_apk

          if [ "$PATCH_KERNELSU" = "ksu-original" ]; then
            APK_VERSION="v0.6.6"
            APK_URL="https://github.com/tiann/KernelSU/releases/download/v0.6.6/KernelSU_v0.6.6_11184-release.apk"
          elif [ "$PATCH_KERNELSU" = "ksu-next" ]; then
            API_RESPONSE=$(curl -s "https://api.github.com/repos/KernelSU-Next/KernelSU-Next/releases/latest")
            APK_VERSION=$(echo "$API_RESPONSE" | jq -r '.tag_name')
            APK_URL=$(echo "$API_RESPONSE" | jq -r \
              '.assets[] | select(.name | test("^KernelSU_Next_v.*_[0-9]+-release\\.apk$")) | .browser_download_url' \
              | head -n1)
          fi

          if [ -n "$APK_URL" ] && [ "$APK_URL" != "null" ]; then
            APK_FILE="manager_apk/$(basename "$APK_URL")"
            curl -LSs -o "$APK_FILE" "$APK_URL"
            echo "MANAGER_APK_PATH=$(pwd)/$APK_FILE" >> $GITHUB_ENV
            echo "MANAGER_APK_VERSION=${APK_VERSION}" >> $GITHUB_ENV
            echo "Downloaded KernelSU Manager APK: ${APK_VERSION}"
          else
            echo "::warning::Could not resolve a Manager APK download URL for PATCH_KERNELSU=${PATCH_KERNELSU}, skipping."
          fi

      # ---------------------------------------------------------------
      # PACKAGING: AnyKernel3 (optional) — zip the contents directly, not the folder itself
      # ---------------------------------------------------------------
      - name: 🎁 Package with AnyKernel3
        if: success() && env.USE_ANYKERNEL3 == 'yes'
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel3
          rm -rf AnyKernel3/.git AnyKernel3/*.zip
          cp "$IMG_PATH" AnyKernel3/
          [ -n "$DTB_IMG_PATH" ] && cp "$DTB_IMG_PATH" AnyKernel3/
          [ -n "$DTBO_IMG_PATH" ] && cp "$DTBO_IMG_PATH" AnyKernel3/
          sed -i "s/device.name1=.*/device.name1=${CODENAME}/" AnyKernel3/anykernel.sh || true
          cd AnyKernel3
          zip -r9 "../${BASE_NAME}.zip" . -x ".git*" -x "*.zip"
          cd ..
          echo "ZIP_PATH=$(pwd)/${BASE_NAME}.zip" >> $GITHUB_ENV

      - name: 📄 Rename raw image
        if: success()
        run: |
          cp "$IMG_PATH" "./${BASE_NAME}-Image.gz-dtb"
          echo "RAW_IMG_PATH=$(pwd)/${BASE_NAME}-Image.gz-dtb" >> $GITHUB_ENV

      # ---------------------------------------------------------------
      # RELEASE: one file = one file, no zip-in-zip
      # ---------------------------------------------------------------
      - name: 🚀 Publish to GitHub Release
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: "${{ env.BASE_NAME }}"
          body: |
            Kernel: ${{ env.KERNEL_NAME }}
            Kernel version: ${{ env.KERNEL_VERSION }}
            Codename: ${{ env.CODENAME }}
            KernelSU: ${{ env.PATCH_KERNELSU }}
            KernelSU Manager: ${{ env.MANAGER_APK_VERSION }}
            Commit: ${{ env.KERNEL_COMMIT }}
          files: |
            ${{ env.RAW_IMG_PATH }}
            ${{ env.ZIP_PATH }}
            ${{ env.MANAGER_APK_PATH }}
            ${{ env.DTB_IMG_PATH }}
            ${{ env.DTBO_IMG_PATH }}

      # ---------------------------------------------------------------
      # NOTIFY: SUCCESS (+ send the file directly to Telegram)
      # ---------------------------------------------------------------
      - name: "✅ Telegram: Notify build success"
        if: success() && env.BOT_TOKEN != '' && env.CHAT_ID != ''
        run: |
          KSU_LABEL="No"
          [ "$PATCH_KERNELSU" = "ksu-next" ] && KSU_LABEL="Yes (KernelSU-Next)"
          [ "$PATCH_KERNELSU" = "ksu-original" ] && KSU_LABEL="Yes (KernelSU Original)"

          SEND_FILE="$ZIP_PATH"
          [ "$USE_ANYKERNEL3" = "no" ] && SEND_FILE="$RAW_IMG_PATH"

          FILE_SIZE=$(du -h "$SEND_FILE" | cut -f1)
          FILE_SHA=$(sha256sum "$SEND_FILE" | cut -c1-12)

          ELAPSED=$(( $(date +%s) - START_EPOCH ))
          DURATION="$((ELAPSED / 60))m $((ELAPSED % 60))s"

          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          RELEASE_URL="${{ github.server_url }}/${{ github.repository }}/releases/tag/${RELEASE_TAG}"

          MANAGER_LINE=""
          [ -n "$MANAGER_APK_VERSION" ] && MANAGER_LINE=$'\n📱 Manager: `'"${MANAGER_APK_VERSION}"'` (bundled in release)'

          MSG=$'✅ *Build Success*\nKernel: `'"${KERNEL_NAME}"'`\nDevice: `'"${CODENAME}"'`\nKernel version: `'"${KERNEL_VERSION}"'`\nKernelSU: `'"${KSU_LABEL}"'`'"${MANAGER_LINE}"$'\n\n⏱ Duration: `'"${DURATION}"'`\n📦 Size: `'"${FILE_SIZE}"'`\n🔗 SHA256: `'"${FILE_SHA}"'`\n📝 Commit: `'"${KERNEL_COMMIT}"'`'

          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
            -d chat_id="${CHAT_ID}" \
            -d parse_mode="Markdown" \
            --data-urlencode text="${MSG}" \
            --data-urlencode reply_markup="$(jq -n --arg run "$RUN_URL" --arg rel "$RELEASE_URL" \
              '{inline_keyboard: [[{text: "🔗 View Run", url: $run}, {text: "⬇️ Download", url: $rel}]]}')"

      - name: "📤 Telegram: Send file"
        if: success() && env.BOT_TOKEN != '' && env.CHAT_ID != ''
        continue-on-error: true
        run: |
          SEND_FILE="$ZIP_PATH"
          [ "$USE_ANYKERNEL3" = "no" ] && SEND_FILE="$RAW_IMG_PATH"
          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendDocument" \
            -F chat_id="${CHAT_ID}" \
            -F document=@"${SEND_FILE}" \
            -F caption="${BASE_NAME}"

      # ---------------------------------------------------------------
      # NOTIFY: FAILED
      # ---------------------------------------------------------------
      - name: "❌ Telegram: Notify build failed"
        if: failure() && env.BOT_TOKEN != '' && env.CHAT_ID != ''
        run: |
          KSU_LABEL="No"
          [ "$PATCH_KERNELSU" = "ksu-next" ] && KSU_LABEL="Yes (KernelSU-Next)"
          [ "$PATCH_KERNELSU" = "ksu-original" ] && KSU_LABEL="Yes (KernelSU Original)"

          ELAPSED=$(( $(date +%s) - START_EPOCH ))
          DURATION="$((ELAPSED / 60))m $((ELAPSED % 60))s"

          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          # Grab the last few lines of build.log so the error is visible right
          # away without opening GitHub Actions. Backticks are stripped so they
          # don't break Telegram's Markdown formatting.
          ERROR_LOG=$(tail -n 8 kernel/build.log 2>/dev/null | tr -d '`' || true)
          [ -z "$ERROR_LOG" ] && ERROR_LOG="(build.log not found, check the run log manually)"

          MSG=$'❌ *Build Failed*\nKernel: `'"${KERNEL_NAME}"'`\nDevice: `'"${CODENAME}"'`\nKernel version: `'"${KERNEL_VERSION}"'`\nKernelSU: `'"${KSU_LABEL}"'`\n⏱ Duration: `'"${DURATION}"'`\n\n📄 *Last error:*\n\`\`\`\n'"${ERROR_LOG}"'\n\`\`\`'

          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
            -d chat_id="${CHAT_ID}" \
            -d parse_mode="Markdown" \
            --data-urlencode text="${MSG}" \
            --data-urlencode reply_markup="$(jq -n --arg url "$RUN_URL" \
              '{inline_keyboard: [[{text: "🔗 View Log", url: $url}]]}')"

      - name: 🧾 Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: kernel/build.log
          if-no-files-found: ignore
